<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <!-- 父项目配置 -->
    <parent>
        <groupId>io.github.youngerier</groupId>
        <artifactId>java-codegen-toolkit</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- 工具包模块信息 -->
    <artifactId>toolkit</artifactId>
    <name>Java Code Generation Toolkit - Common</name>
    <description>通用工具类和基础组件，用于Java代码生成</description>

    <dependencies>
        <!-- ============================ -->
        <!-- 代码增强工具 -->
        <!-- ============================ -->
        
        <!-- Lombok - 代码简化工具 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        
        <!-- ============================ -->
        <!-- 核心依赖（compile，向使用方传递） -->
        <!--   spring-context 已传递 spring-core/beans/aop/expression -->
        <!-- ============================ -->

        <!-- Spring 上下文：MessageSource、@Configuration、Spring 工具类 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <!-- Bean 验证 API（分页入参 @NotNull 等） -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <!-- ============================ -->
        <!-- 可选集成（optional，不向使用方传递） -->
        <!--   自动装配中按 @ConditionalOnClass / @ConditionalOnWebApplication 按需生效 -->
        <!-- ============================ -->

        <!-- MyBatis Flex：仅 QueryWrapperHelper 使用，codegen 生成的 Repository 也需要 -->
        <dependency>
            <groupId>com.mybatis-flex</groupId>
            <artifactId>mybatis-flex-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Web / WebMVC：GlobalExceptionHandler、TraceIdFilter -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Security：SecurityExceptionHandler 条件装配，使用方无 security 时不生效 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
            <version>${spring-security.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Servlet API -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- ============================ -->
        <!-- MQ 支持，可选依赖 -->
        <!-- 使用 support.trace.rocketmq 下的 Hook / 自动装配时需自行引入 -->
        <!-- ============================ -->

        <!-- RocketMQ Client：TraceSendMessageHook / TraceConsumeMessageHook -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-client</artifactId>
            <version>${rocketmq.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- RocketMQ Spring：识别 RocketMQTemplate 与 @RocketMQMessageListener 容器 -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-spring-boot</artifactId>
            <version>${rocketmq-spring.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- ============================ -->
        <!-- 日志框架 -->
        <!-- ============================ -->
        
        <!-- SLF4J -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- ============================ -->
        <!-- Spring Boot 自动装配（仅编译期，使用方 Boot 环境已自带） -->
        <!-- ============================ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <version>${spring-boot.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- ============================ -->
        <!-- Office (Excel) 支持，可选依赖 -->
        <!-- 使用 support.office 下的类时需自行引入 -->
        <!-- ============================ -->

        <!-- EasyExcel - Excel 读写 -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>easyexcel</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Commons Lang3 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Swagger 注解 -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- 提供真实 MDC 实现，生产环境由使用方的日志框架（logback 等）提供 -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <!-- ============================ -->
    <!-- 构建配置 -->
    <!-- ============================ -->
    <build>
        <plugins>
            <!-- 编译插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            
            <!-- 源码打包插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            
            <!-- Javadoc插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>